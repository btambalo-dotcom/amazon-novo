
# Porta local
FLASK_RUN_PORT=5000
# URL do banco de dados (ajuste em produção)
# Local (arquivo na pasta instance/)
DATABASE_URL=sqlite:///instance/app.db
# Produção (Render) — lembre-se de montar /data com disco persistente
# DATABASE_URL=sqlite:////data/app.db
# Se quiser secret key customizada
SECRET_KEY=troque-esta-chave
